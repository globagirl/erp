//Récupération des valeurs d'un checkBox
//JavaScript
var valeurs = [];
   $("input:checked[name=client]").each(function (box) {
	  valeurs.push($(this).val());
   });
//Checkbox checked
 if ($('#id').is(':checked')) {
   //traitement
}
//Multiple
var $boxes = $('input[name=thename]:checked');
//checked value
var statut = document.querySelector('input[name=statut]:checked').value;
//PHP MYSQL: x in ($rest)
//Ouvrier dans un nouveau angle via js
window.open("url", '_blank');
///PHP
if (!mysql_query($sql)) {
die('Error: ' . mysql_error());

}else{...}
